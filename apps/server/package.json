{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b && tsc -p tests/tsconfig.json",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "test": "bun test",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@CC-City-Chauffeurs/auth": "workspace:*",
    "@CC-City-Chauffeurs/core": "workspace:*",
    "@CC-City-Chauffeurs/db": "workspace:*",
    "@CC-City-Chauffeurs/env": "workspace:*",
    "@CC-City-Chauffeurs/whatsapp": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.32",
    "nodemailer": "^10.0.10",
    "sharp": "^0.35.4",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@CC-City-Chauffeurs/config": "workspace:*",
    "@electric-sql/pglite": "^0.5.8",
    "@types/bun": "catalog:",
    "@types/nodemailer": "^8.0.2",
    "tsdown": "^0.22.14",
    "typescript": "catalog:"
  }
}
